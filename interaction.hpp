#ifndef TRIVIAL_INTERACTION_HPP
#define TRIVIAL_INTERACTION_HPP

namespace trivial
{

    struct interaction
    {
        enum result_type
        {
            NONE,
            MERGE
        };
    };

}

#endif
